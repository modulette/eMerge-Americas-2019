{
  "name": "emerge",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "axios": "^0.18.0",
    "express": "^4.16.4",
    "helmet": "^3.16.0",
    "install": "^0.12.2",
    "jsonserver": "^0.1.6",
    "mongoose": "^5.5.4",
    "npm": "^6.9.0",
    "react": "^16.8.6",
    "react-dom": "^16.8.6",
    "react-scripts": "3.0.0",
    "remarkable": "^1.7.1",
    "socket.io": "^2.2.0"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 0",
    "start": "node server.js && npm run json:server",
    "client": "cd client && yarn build",
    "json:server": "json-server --watch data.json --port 4001"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}